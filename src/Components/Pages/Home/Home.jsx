import PropTypes from 'prop-types';
import Banner from '../Banner/Banner';

const Home = props => {
    return (
        <div>
            <header>
                <Banner></Banner>
            </header>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;